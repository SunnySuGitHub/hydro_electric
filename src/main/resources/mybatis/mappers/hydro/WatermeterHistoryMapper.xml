<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hust.hydroelectric_backend.Dao.hydro.WatermeterHistoryMapper">
    <!--mapper接口所在包-->

    <select id="getWatermeterHistoryByMeterNo" resultType="com.hust.hydroelectric_backend.Entity.Watermeters.WatermeterHistorydata">
        select
          meter_no,
          read_value,
          read_time,
          enprNo
        from
          t_watermeter_historydata
        where
          meter_no = #{meterNo}
          and enprNo = #{enprNo}
        <![CDATA[
            and read_time >= #{startLine}
            and read_time <= #{endLine}
        ]]>
    </select>

    <select id="getLatestRecordByMeterNo" resultType="java.math.BigDecimal">
        select
          read_value
        from
          t_watermeter_historydata
        where
          meter_no = #{meterNo}
          and enprNo = #{enprNo}
        order by
          id desc
        limit 1
    </select>


</mapper>